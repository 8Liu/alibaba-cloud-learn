server:
  port: 8092
spring:
  application:
    name: sentinel-feign-provider
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
    sentinel:
      transport:
        dashboard: 127.0.0.1:8080
        port: 8719
feign:
  sentinel:
    enabled: true